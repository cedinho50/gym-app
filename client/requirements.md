## Packages
framer-motion | Page transitions and smooth interactive component animations
canvas-confetti | Celebration effect when completing workouts
@types/canvas-confetti | Types for the confetti package

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["var(--font-sans)"],
  display: ["var(--font-display)"],
}

The user explicitly requested a "cleanem weiss" (clean white), strictly light mode, minimalist design. We will define both light and dark CSS variables to be identical to force this bright, airy aesthetic regardless of system preferences.
